To build executables in bin/:
  $ make all

To run, e.g. fib(39) on 4 procesors
  $ bin/fib @mpl procs 4 -- -N 39

============================================================================
Examples

fib -N n
calculates nth Fibonacci number with naive recursive formula

tabulate -N n
generate array of n random characters


