default:
	../../../build/bin/mlton-parmem -default-ann 'allowFFI true' -cc-opt -g -link-opt -lX11 -output game game.mlb

debug:
	../../../build/bin/mlton-parmem -debug true -debug-runtime true -keep g -default-ann 'allowFFI true' -cc-opt -g -link-opt -lX11 -output game-debug game.mlb
